# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
.venv/
venv/
env/

# Logs
*.log

# Torch checkpoints / model artifacts
*.pt
*.bin
*.safetensors
*.pth
*.ckpt
*.bin
*.tar
*.zip
*.wav
*.flac
*.log

# TensorBoard / experiment outputs
runs/
tensorboard/
ASR/zipformer/exp/
ASR/zipformer/exp_*/
ASR/zipformer/cross_eval_*/
exp/
exp_*/
# Exported model packages: keep the deployed medium model in full (needed for
# `git clone` + run.sh on a fresh Jetson to work with no other file transfer).
# Other model_*/ dirs (backups, alternates) keep only metadata.
deploy/jetson_nano/model_*/*
!deploy/jetson_nano/model_medium_epoch30_avg10/**
!deploy/jetson_nano/model_*/MODEL_INFO.txt
!deploy/jetson_nano/model_*/tokens.txt
deploy/jetson_nano/perf_results/
deploy/jetson_nano/remote_mic_ui/ui_uploads/
deploy/jetson_nano/remote_mic_ui/collected/
deploy/*.tar.gz

# Lhotse / feature artifacts
fbank/
fbank_*/
fbank_nr/
manifests/
manifests_fixed/
data/manifests_*/

# Raw / generated speech data
audio/
audio_*/
audio_nr/
datasets/
dataset_backup_*/
heldout_speaker_eval/
dataset/
musan/
musan_data/
MUSAN/
pretrained/
mic_debug/
mic_prompt_eval/
musan_cuts.jsonl.gz

# Generated metadata / language resources
data/
lang/
transcripts/
# Generated transcript versions (rebuildable from dataset/ + the clone scripts).
# transcripts_matched_u20/ (ground-truth labels for the real recordings) is
# also ignored: dataset/ (the audio it points to) isn't tracked either, so
# labels alone aren't useful without the wavs.
transcripts_*/
transcripts_nr/
README_PREPARED.txt
presentation_info.md

# OS / editor
.DS_Store
.vscode/
.idea/

theory_*.md

# Speaker-ID embedding weight: 27 MB, downloadable from sherpa-onnx releases.
# Kept out of git; see live_ui/README.md for the fetch command. The small
# enrolled_speakers.json (derived vectors) and silero_vad.onnx stay tracked.
live_ui/models/speaker_embedding_campplus.onnx
deploy/jetson_nano/sweep_*/

docs/